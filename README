使い方

TransitQuery query = new TransitQuery();
query.setFrom("品川");
query.setTo("東京");
TransitSearcher searcher = TransitSearcherFactory.createMobileSearcher(Platform.GENERIC);
TransitResult result = searcher.search(query);
